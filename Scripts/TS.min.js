webpackJsonp([408], {
  10181: function(t, o) {
    (function() {
      "use strict";
      if (void 0 !== window.TS) {
        if (TS.info) TS.info("TS.min.source is not needed when TS.source is included.");
        return;
      }
      var t = {};
      window.TS = {
        registerModule: function o(e, r) {
          var n = e.split(".");
          var i = TS;
          var a = 0;
          var c = n.length - 1;
          for (a; a < c; a += 1) {
            e = n[a];
            if (void 0 === i[e]) i[e] = {};
            i = i[e];
          }
          e = n[a];
          a += 1;
          i[e] = r;
          t[e] = r;
        },
        boot: function t(o) {
          TS.boot_data = o;
        },
        exportToLegacy: function t(o, r) {
          if (_.has(TS, o)) throw new Error("exportToLegacy: there is already something at " + o + "; we cannot overwrite it");
          if (!o || 0 !== o.indexOf("interop")) throw new Error("exportToLegacy: Name " + o + ' must start with "interop".');
          if (r.onStart) throw new Error("exportToLegacy: may not export objects with onStart methods");
          if (e) _.set(TS, o, r);
          else TS.registerModule(o, r);
        }
      };
      var o = function o() {
        Object.keys(t).forEach(function(o) {
          var e = t[o];
          if (!e.onStart) return;
          e.onStart();
        });
      };
      var e = false;
      var r = function t() {
        o();
        e = true;
      };
      $(document).ready(r);
    })();
  }
}, [10181]);
